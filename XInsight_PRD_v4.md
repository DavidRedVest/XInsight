# XInsight 软件需求文档 (PRD v4.2)

> 一个面向 macOS 的、Source Insight 风格的代码阅读器 + 轻量编辑器。
> 三大基石:**光标跟随的上下文窗口** · **任意方向的灵活分屏** · **快速代码搜索**。
> 架构双核心:**tree-sitter 零配置默认层 + clangd 可选精确层**,由统一 router 调度。
> 工程原则:**纯 C++ 核心 + 可替换 GUI**,核心不依赖任何界面框架。
> 本文档可直接交给 Claude Code 分阶段实现。
>
> 本 v4 **完整替代 v3**。相对 v3 的关键变化:(1) **定位从"只读浏览器"改为"可编辑轻量编辑器"**——支持编辑、保存、新建文件/文件夹、另存为,并据此正式纳入文档同步/撤销/外部改动等复杂度(见第 2.3、8.7 节);(2) 消除若干"骑墙/留白"规格点——量化验收基准、修正 tree-sitter 候选排序不可达的指标、明确 `ProjectModel` 的文件纳入规则、了结 query 许可问题。
>
> **v3 → v4 编辑能力边界(先读这条,避免误解):** v1 交付的是"能顺手改、能存"的轻量编辑,**不是** IDE 级编辑器。明确**做**:文本编辑、`Cmd+S` 保存、新建文件/文件夹、另存为、撤销/重做、编辑后索引增量更新。明确**不做**(留 v2+):符号级 rename(属 clangd 语义重构)、自动补全(属 clangd)、多光标/列编辑、宏录制、代码格式化。
>
> **v4 → v4.1 增量:** (1) 固化 query 编写策略——tags/folds 自写、highlights 裁剪社区版(8.1);(2) 主题改为数据驱动、可加载,内置深色/浅色/**护眼**三套并支持自定义主题文件,且要求 M1 就把上色做成可加载结构(P1 #14、M1)。
>
> **v4.1 → v4.2 增量:** (1) **多编码**——UTF-8/GBK/GB18030/Latin-1 自动检测、按原编码写回(2.3,嵌入式遗留中文注释代码的刚需);(2) 上下文窗格补 **SI4 式变量→类型解码**(2.1);(3) 符号重索引**两级防抖**(8.7);(4) 新增 **`.xinsight/` 项目配置目录 + 最小设置系统**(5.7);(5) 补齐若干决定注记——dirty 会话恢复策略(P1 #11)、超大文件只读降级(5.6)、刁钻样本测试夹具(第 10 节)、打包公证(第 9 节)。

---

## 0. 一句话定位

> 一个**以"看代码"为第一优先级、同时支持轻量编辑**的代码工具:把 Source Insight "打开任何烂摊子代码都能零配置跳转、光标停在符号上就看到实现"的体验搬到 macOS,补上它缺的**左右分屏**,让你在读代码之余能顺手改、能存、能新建文件,并在用户愿意配置时叠加 clangd 的精确能力。

它不是 IDE,不做编译/调试/语义重构(rename 等留给可选的 clangd 层),不做自动补全/格式化。它把"读大型 C/C++ 代码的导航、搜索、理解速度"做到极致,同时提供**够用、不碍事的编辑**(编辑、保存、新建、撤销)。

---

## 1. 背景与动机

- Source Insight 4 是 Windows 上极好的代码阅读工具,但没有 macOS 版本。
- 作者主力机为 macOS (Apple Silicon),日常阅读大型 C/C++(嵌入式 BSP、音视频管线、STM32 裸机工程等)。
- **Source Insight 的护城河洞察**(本 PRD 的立项依据):它的杀手锏不是编辑器,而是一套**容错的、不需要能编译的模糊解析器 + 实时符号库**。它不依赖 `compile_commands.json`、不需要 include/宏配全,代码编译不过照样建符号表、照样跳转。**"零配置、打开即用、能读残缺代码"才是它让人用过不想换的根本。**
- 现代 clangd/LSP 走的是相反路线:追求编译级精确,因而**依赖可编译环境与配置**(STM32 裸机要 `--query-driver` + `.clangd`,Makefile 要 `bear`……)。若 XInsight 只架在 clangd 上,就会继承这份配置负担,恰好丢掉 Source Insight 的灵魂。
- **结论:XInsight 用 tree-sitter(现代版的"Source Insight 式容错解析")做默认层夺回零配置体验,把 clangd 作为可选精确叠加层,两条路都占。**

---

## 2. 核心差异化功能

产品的灵魂。评审任何实现,先看这两点是否达标。

### 2.1 光标跟随的上下文窗口 (Ambient Context Pane)

- 主编辑区光标移到任意符号(函数、类型、宏、变量)上时,**下方/侧边窗格自动、即时**显示该符号的**定义源码片段**。
- 关键词是"**自动**":不点击、不按键。与 VS Code "Peek Definition"(主动触发)的区别在于它是**环境光式**、零操作的。
- **由默认层(tree-sitter)驱动**:因此它**永不等待 clangd 或任何配置**,任何项目打开即生效。
- 行为细节:
  - 光标停留触发,**防抖 150–250ms**;移动中取消在途过期请求。
  - 窗格显示目标符号所在文件的源码区间(函数体或定义行 ± 若干行),高亮定义行,顶部显示 `文件:行号  签名`。
  - **变量→类型解码(对齐 SI4)**:光标落在变量上时,不止显示变量声明,而是**顺着声明找到其类型并显示该类型的定义**。例如 `struct S *psvar` → 显示 `struct S` 的定义。tree-sitter 可近似实现:从声明节点取类型名,再查符号索引里该类型的定义;做不到时回落为显示变量声明本身。这是 SI4 Context 窗口的标志性行为之一,值得复刻。
  - 支持**点击进入 (drill-down)**:设为新焦点,上一处压入返回栈。
  - **多候选定义**是常态(名字匹配天然会命中多个):窗格顶部给可切换的候选列表,按"同文件 > 同目录 > 全项目"位置就近排序,默认选最可能的一个。"优先定义、其次声明"作为**尽力而为**的次级排序(tree-sitter 只看语法,定义/声明的区分不总可靠,故不作硬保证);**精确的定义/声明区分由 clangd 层负责**,就绪时用其结果。
  - 可选项 "prefer precise":clangd 就绪时,用其精确结果覆盖候选(默认关闭,避免闪烁;开则更准)。

### 2.2 任意方向的灵活分屏 (Flexible Split)

- 支持**左右分屏**与**上下分屏**,可**嵌套**(左一列、右再上下分),补齐 Source Insight 4 的短板。
- 每个格子是独立编辑视图,各自打开不同文件、独立滚动。
- 上下文窗格本身也是可停靠区域,位置(下方/右侧)可切换。
- 快捷键:`Cmd+\` 左右、`Cmd+Shift+\` 上下、`Cmd+W` 关闭当前格子。

### 2.3 轻量编辑 (P0)

> v1 的编辑目标是"顺手改、能存、不碍事",范围**严格限定**如下,防止滑向 IDE。

- **做**:
  - 文本编辑(输入、删除、剪切/复制/粘贴、行操作)。
  - **保存**:`Cmd+S` 保存当前文件;标题/标签页显示"已修改 (dirty)"标记;关闭 dirty 文件/退出前提示保存。
  - **新建**:新建文件、新建文件夹(在文件树右键或菜单);**另存为**(`Cmd+Shift+S`)。
  - **撤销/重做**:`Cmd+Z` / `Cmd+Shift+Z`,每个文件独立的撤销栈(QScintilla 自带,接好即可)。
  - **编辑联动索引**:文件内容变化 → tree-sitter 增量重解析该文件 → 更新其索引切片(见 5.3、8.7);若 clangd 在用则发 `didChange`。这保证改完符号后跳转/大纲/上下文窗格立即反映新状态。
- **不做(留 v2+,写清以防蔓延)**:符号级 rename(属 clangd 语义重构,见 P1 #15)、自动补全、代码格式化、多光标/列编辑、宏录制、代码片段(snippets)。
- **外部改动处理**:文件在 XInsight 之外被改动(如 git 切分支)时,检测到后**提示重新加载**;若本地有未保存修改则提示冲突(保留本地 / 放弃本地 / 取消),不静默覆盖。v1 做到"检测 + 提示"即可,不做三方合并。
- **编码/换行(多编码,非仅 UTF-8)**:v1 打开文件时**自动检测编码**,至少支持 **UTF-8、GBK/GB18030、Latin-1**;能打开、编辑,并**按原编码写回**(不擅自转码)。理由:嵌入式遗留代码大量是 GBK/GB2312 带中文注释,若只支持 UTF-8,作者自己最常碰的文件将无法编辑保存——故多编码为 v1 必做项,不是可选。保留文件原有换行风格(LF/CRLF);状态栏显示并允许手动切换当前文件的编码与换行。检测不确定时以 UTF-8 优先并提示,不静默猜错致乱码保存。

---

## 3. 搜索功能设计 (P0 基础功能)

分两类,统一到一个结果面板。**符号类搜索由默认层(tree-sitter 索引)服务,clangd 就绪时升级为精确。**

### 3.1 文本搜索(不依赖任何解析器)

- 全项目字面量 / 正则搜索,后端**直接外挂 ripgrep**(不自研搜索引擎)。
- 用 `rg --json`:**逐行 JSON 事件流**(`begin`/`match`/`context`/`end`/`summary`)。解析 `match` 取 `path`、`line_number`、行文本、`submatches`(命中区间用于高亮)。
- **流式**出结果、可随时取消;大结果集可截断/分页。
- 选项:大小写敏感、全词、正则、glob 过滤;是否遵循 `.gitignore`(默认遵循,**必须可关**——嵌入式常要搜进被忽略目录)。

### 3.2 符号搜索

- **工作区符号**(`Cmd+T`):默认走 **tree-sitter 工作区符号索引**(见 5.3),clangd 就绪时升级为 `workspace/symbol`。
- **当前文件符号**(`Cmd+Shift+O`):走 tree-sitter 当前文件解析,喂 Outline 侧栏。
- **查引用**(`Shift+F12`):默认走 tree-sitter 的标识符出现索引(模糊,基于名字),clangd 就绪时升级为精确 `references`。结果注明"精确/快速"。

### 3.3 统一搜索结果面板

- 文本、符号、查引用**共用**一个可停靠面板,统一模型 `SearchResult { path, line, col, preview, kind, precise:bool }`。
- 按文件分组、可折叠;**双击/回车跳转**,走统一 `NavigationEngine`(与返回栈一致)。
- 高亮命中片段;顶部显示命中数、耗时、以及本次是精确还是快速结果。

---

## 4. 技术选型:C++ 核心 + 双层代码智能 + 可替换 GUI

> **原则:业务逻辑全部落在纯 C++ 核心库;GUI 只是一层可替换适配;代码智能由 tree-sitter 与 clangd 双引擎经 router 提供。** v1 GUI 用 Qt 6,但核心不得依赖 Qt。

### 4.1 核心库(`xinsight-core`,纯 C++20)

- 依赖:标准库、`tree-sitter` + `tree-sitter-c` + `tree-sitter-cpp`(C 库,直接链入核心)、`nlohmann/json`(LSP 消息)、`reproc`(跨平台子进程,启动 clangd/ripgrep)。
- **不依赖任何 GUI 框架**(不链 Qt)。
- 承载:双层代码智能(tree-sitter 引擎 + clangd 客户端 + router)、搜索引擎、项目模型、导航栈、上下文引擎等全部逻辑。
- **验收铁律**:`xinsight-core` 必须能在**不链接任何 GUI 库**的情况下编译,并有独立的无界面测试可执行文件跑通(对 tree-sitter 做零配置跳转、对真实 clangd 做精确跳转、对 ripgrep 做搜索)。这条决定"解耦"是真是假。

### 4.2 双层代码智能

**Layer A · tree-sitter(默认,零配置,永远在线)**
- 现代版的"Source Insight 式容错解析":增量、容错、不需要能编译、极快,天生解析残缺代码。
- 负责:语法高亮、折叠、当前文件大纲、以及**自建工作区符号索引**(全项目跳转/查引用/符号搜索的默认后端)。
- 局限(需向用户诚实呈现):它只懂**语法不懂语义**,不做重载决议/模板实例化/精确宏展开,故跳转是**基于名字**、可能多候选。这与 Source Insight 的取舍一致:**换来的是鲁棒与速度。**

**Layer B · clangd(可选,精确,按需叠加)**
- 仅当项目存在/配好 `compile_commands.json` 时启用。
- 把跳转、查引用、工作区符号升级为**编译级精确**;并解锁 tree-sitter 给不了的:call hierarchy、rename、诊断、精确 hover/签名、semantic tokens。
- **绝不阻塞默认体验**:未配置/未就绪时,一切静默回落到 tree-sitter,无死菜单、无"未找到"(只要 tree-sitter 有答案)。

**能力矩阵**

| 操作 | tree-sitter(默认) | clangd(叠加,精确) |
|---|---|---|
| 语法高亮 | ✅ 默认 | semantic tokens 升级 |
| 折叠 | ✅ | — |
| 当前文件大纲 | ✅ | ✅(更准) |
| 跳转定义 | ✅ 名字匹配(可能多候选) | ✅ 精确 |
| 查引用 | ✅ 标识符出现(模糊) | ✅ 精确 |
| 工作区符号搜索 | ✅ 自建索引 | ✅ |
| hover/签名 | 部分(取声明文本) | ✅ |
| call hierarchy | 近似/困难 | ✅ |
| rename | ❌(不安全) | ✅ |
| 诊断 | ❌ | ✅ |

### 4.3 GUI 前端(v1 = Qt 6)

- Qt 6 (Widgets) + **QScintilla** 作文本控件 + 嵌套 `QSplitter`(灵活分屏)。
- **高亮由 tree-sitter 驱动**(把 tree-sitter highlight query 结果映射到 Scintilla styling/indicator),Scintilla 内置词法器仅作无 grammar 语言的兜底。
- 只做"渲染 + 输入 + 把用户意图转成对核心的调用",**不含 LSP JSON 拼装、不含 ripgrep/tree-sitter/clangd 直调**。
- 选 Qt 理由:作者最强项、跨平台、编辑器与分屏组件成熟。

### 4.4 备选 GUI(仅记录)

- 核心已解耦,将来可换原生 Cocoa/SwiftUI、Dear ImGui、Slint 等,**不改核心**。v1 不做。

---

## 5. 架构设计(端口-适配器 / 分层 / 双引擎路由)

依赖方向**单向向下**:GUI → 适配器 → 核心。核心**永不**反向依赖 GUI。

```
┌───────────────────────────────────────────────────────────┐
│  Layer 3 · GUI 前端 (Qt 6, 可替换)                          │
│  MainWindow · 编辑视图(QScintilla,tree-sitter 上色)        │
│  SplitManager(QSplitter) · 上下文窗格 · 搜索结果面板        │
│  文件树 / Outline 视图 · clangd 配置诊断面板                │
│  —— 只调用核心公开 API,只渲染核心给的数据 ——              │
├───────────────────────────────────────────────────────────┤
│  Layer 2 · 平台适配 (thin)                                  │
│  QtUiDispatcher : 实现 IUiDispatcher,把结果 post 回 UI 线程 │
├───────────────────────────────────────────────────────────┤
│  Layer 1 · xinsight-core (纯 C++20,零 GUI 依赖)            │
│                                                             │
│   ┌─────────────── CodeIntelligence (router) ───────────┐  │
│   │  按策略把 定义/引用/符号/hover 路由给最优 provider   │  │
│   │      ┌──────────────────┐   ┌────────────────────┐  │  │
│   │      │ TreeSitterEngine │   │ ClangdProvider     │  │  │
│   │      │ (默认,零配置)   │   │ (可选,精确,LSP)  │  │  │
│   │      │ 高亮/大纲/折叠   │   │ LspClient: JSON-RPC │  │  │
│   │      │ 工作区符号索引   │   │ /stdio 帧解析+握手  │  │  │
│   │      └──────────────────┘   └────────────────────┘  │  │
│   └──────────────────────────────────────────────────────┘  │
│   SearchEngine(ripgrep + 符号搜索) · ProjectModel           │
│   NavigationEngine(跳转/返回栈) · ContextEngine(防抖+预取)  │
│   依赖: tree-sitter(+c/+cpp) · nlohmann/json · reproc · std │
├───────────────────────────────────────────────────────────┤
│  外部进程:clangd · ripgrep   (由 reproc 启动)             │
└───────────────────────────────────────────────────────────┘
```

### 5.1 核心与 GUI 的唯一缝合点:UI 派发器

- 核心自有 I/O 线程(读写 clangd/ripgrep、后台建 tree-sitter 索引),**不借用 GUI 事件循环**。
- 唯一出站端口:

  ```cpp
  struct IUiDispatcher {
      virtual void post(std::function<void()> fn) = 0; // 在 UI 线程执行 fn
      virtual ~IUiDispatcher() = default;
  };
  ```
- Qt 前端提供 `QtUiDispatcher`(内部用 `QMetaObject::invokeMethod`/队列信号)。换 GUI 只重写这一个类。
- 核心异步接口用 `std::function` 回调,**不用 Qt signal**。

### 5.2 CodeIntelligence 路由策略(交给 Claude Code 的确定性规则)

- **高亮、折叠、当前文件大纲**:**永远 tree-sitter**(每文件、即时,无理由等 clangd)。高亮可在用户开启时升级为 clangd semantic tokens。
- **跳转定义 / 查引用 / 工作区符号 / hover**:
  - 若 `clangd 已配置` 且 `该 TU 已完成索引` → 用 clangd(标记 `precise=true`);
  - 否则 → 用 tree-sitter(标记 `precise=false`)。
- **上下文窗格**:默认始终用 tree-sitter 即时出结果(永不闪烁等待);仅当用户开启 "prefer precise" 且 clangd 已就绪时,用精确结果覆盖候选。
- **降级铁律**:clangd 缺席/未就绪时,任何操作都不得报"未找到"或禁用,只要 tree-sitter 能答就答;UI 用一个小标识展示当前是"精确"还是"快速"模式,让用户知道结果可能模糊。

### 5.3 tree-sitter 工作区符号索引(Source Insight 式符号库的等价物)

- 启动/打开项目时,后台遍历源文件,用 tree-sitter 解析,跑 **query(.scm)** 抽取定义:函数、方法、类/结构体/枚举/联合、typedef、宏、全局变量;同时抽取标识符出现用于查引用。
- 存入索引:`符号名 → [{file,line,col,kind,container}]`,以及标识符倒排。
- **增量**:文件改动只重解析该文件、只更新其索引切片(tree-sitter 增量解析)。
- 跳转即"按名字查索引 + 候选排序";多候选交给上下文窗格的候选列表(2.1)。**这就是 Source Insight 那种模糊但永远能用的符号库。**

#### 5.3.1 索引存储:v1 交付范围(明确规定,不得含糊)

> 这里是一个容易被 AI 猜测的规格点,故写死。

- **抽象要求(v1 必做)**:索引存储必须置于统一接口 `ISymbolIndex` 之后(定义、查询、增量更新、清空等操作全走接口)。上层(`CodeIntelligence`、`SearchEngine`、`ContextEngine`)**只依赖接口,不依赖任何具体后端**。
- **实现要求(v1 只做一个)**:**v1 仅交付内存后端 `InMemorySymbolIndex`**(实现 `ISymbolIndex`)。几十万行量级下符号条目轻量(每条约几十字节),内存占用仅数十 MB,内存后端已完全够用,**不做提前优化**。
- **SQLite 后端 = 明确的非 v1 目标**:`SqliteSymbolIndex` 为将来预留,v1 **不实现**。其唯一真实价值是"重启免重建索引";等该痛点真实出现时再加一个实现类即可,**不构成对 v1 数据模型或上层的返工**。
- **抽象质量的验收判据**:为 `ISymbolIndex` 写一套**与后端无关的接口层单测**,v1 用 `InMemorySymbolIndex` 跑通;判据是"**将来 `SqliteSymbolIndex` 落地后能复用同一套单测直接通过**"。若一套测试无法同时套住两个实现,说明接口没抽干净,需重抽。
- **内存后端的必然代价(需在 M2 一并处理)**:内存索引意味着**每次启动都要重新扫描建索引**(不同于 SI4 的持久化)。因此 v1 必须把"后台建索引 + 进度提示 + 索引期间功能可用(先给已索引部分的结果)"做扎实,保证启动体验不难看。这部分工作已归入里程碑 M2。

### 5.4 模块归属

| 模块 | 归属 | 说明 |
|---|---|---|
| CodeIntelligence(router) | 核心 | 代码智能唯一出口,按 5.2 路由 |
| TreeSitterEngine | 核心 | 高亮/大纲/折叠/工作区符号索引 |
| ClangdProvider / LspClient | 核心 | 与 clangd 通信唯一出口 |
| SearchEngine | 核心 | ripgrep + 符号搜索唯一出口 |
| ProjectModel | 核心 | 文件树数据、compile db 定位、索引状态 |
| NavigationEngine | 核心 | 跳转/前进后退栈(纯数据) |
| ContextEngine | 核心 | 光标→防抖→取定义→发出源码区间 |
| SplitManager | GUI | QSplitter 树;布局用核心 plain struct 序列化 |
| 各 widget/view | GUI | 只渲染核心数据、只把输入转成核心调用 |

### 5.5 架构纪律(硬约束)

- **所有代码智能查询只能经 `CodeIntelligence`**;GUI 与其它核心模块**禁止**直接调 tree-sitter / clangd。
- GUI 层禁止出现:LSP JSON 拼装、`rg`/`clangd` 进程启动、compile db 解析、tree-sitter 直调。
- 跳转只经 `NavigationEngine`;搜索只经 `SearchEngine`;LSP 只经 `LspClient`。
- 核心禁 `#include <Q...>`;构建里单独编译核心+测试且不链 Qt。

### 5.6 ProjectModel:文件纳入规则(明确规定,勿擅自拍板)

- **纳入索引/文件树的扩展名(默认)**:C/C++ 源与头 —— `.c .cc .cpp .cxx .c++ .h .hh .hpp .hxx .h++ .inl`(大小写不敏感)。这套集合应可在设置里增删。
- **默认跳过的目录**:`.git .svn .hg`、`build build-* out cmake-build-*`、`node_modules`、`.cache`。可在设置里增删;跳过规则对文件树与索引都生效。
- **符号链接**:默认**不跟随**符号链接目录(避免环与重复索引);跟随与否作为设置项。
- **文件大小上限**:超过阈值(默认如 5 MB)的单文件跳过解析索引(仍可在文件树点开只读查看),避免个别巨型生成文件拖垮索引。阈值可配。
- **打开超大文件的降级(决定注记)**:5.6 只处理"跳过索引";编辑器**打开**超大文件(如 >20 MB 的生成文件/日志)时,v1 降级为**只读 + 不做 tree-sitter 上色**,避免 QScintilla 卡死。阈值可配。
- **二进制/非文本**:按内容或扩展名跳过明显的二进制文件。
- 以上均为**可配置的默认值**,不是硬编码;但 v1 必须带上这套合理默认,不能留空让用户从零配。

### 5.7 配置与状态的归属:`.xinsight/` 项目目录

> 文档里"可配置"出现多处(文件纳入、gitignore 开关、主题、追踪选项、clangd 路径……),需要一个统一的家,否则配置散落无处安放。

- 每个打开的项目根下用一个 **`.xinsight/` 目录**收拢项目级状态:分屏/标签布局(会话恢复)、clangd 相关配置、文件纳入规则的项目级覆盖、以及**将来**的索引缓存(如 SQLite 落地时)。
- **全局配置**(与项目无关:主题选择、自定义主题文件、全局默认追踪选项等)放用户级配置目录(如 `~/Library/Application Support/XInsight/`)。项目级可覆盖全局。
- v1 需要一个**最小设置系统**(读写这些配置的统一入口,置于核心、经接口暴露),不要求做完整设置 UI——但存取要走这一处,别让各模块各写各的文件。
- `.xinsight/` 应可被用户加进 `.gitignore`;文档提示这一点。

---

## 6. 功能需求(按优先级)

### P0(MVP 必须)

1. 打开项目目录,显示文件树。
2. Tab / 分屏打开文件,**tree-sitter 语法高亮**。
3. **tree-sitter 工作区符号索引**建立(后台,显示进度),支撑零配置跳转/搜索。
4. **光标跟随上下文窗格**(2.1),tree-sitter 驱动——核心卖点、零配置。
5. **左右 + 上下 + 嵌套分屏**(2.2)。
6. **代码搜索**(第 3 节):文本(ripgrep,流式)+ 工作区符号(tree-sitter)+ 统一结果面板。
7. 跳转定义(`F12`)、查引用(`Shift+F12`)、当前文件大纲、`Cmd+P/T/Shift+O` 快速跳转,**全部零配置可用**。
8. 返回/前进栈(`Cmd+[ / Cmd+]`)。
9. **轻量编辑**(2.3):编辑、`Cmd+S` 保存、新建文件/文件夹、另存为、撤销/重做、dirty 标记、外部改动重载提示;编辑后索引增量同步。
10. **clangd 精确叠加层**:检测/启用 clangd,就绪后把跳转/引用/符号升级为精确,并提供 call hierarchy;含"clangd 配置诊断"面板与精确/快速模式标识。

### P1(让它好用)

11. 会话恢复:文件与分屏布局。**dirty 文件的处理(决定注记)**:v1 恢复布局但**不做未保存内容的热恢复**——重开时对上次未保存的文件,重新从磁盘加载并提示"上次有未保存修改";正常退出前已有关闭保存提示兜底。热退出(保留未存改动)留 v2。
12. 符号高亮:光标所在标识符,同文件其它出现处高亮(tree-sitter)。
13. 面包屑:`命名空间 > 类 > 函数`。
14. **主题(数据驱动)**:编辑器 UI 颜色(背景/前景/当前行/选中/行号)+ tree-sitter token 类别颜色,统一由**可加载的主题定义**提供(非硬编码)。v1 内置三套:**深色、浅色、护眼**(护眼给具体基线,如豆沙绿 `#C7EDCC` 底 + 低对比前景,或内置一套 Solarized);支持用户**加载自定义主题文件**(放入指定目录即生效),护眼配色可自行微调到最舒服。
15. 搜索增强:历史、结果内再过滤。
16. **clangd 层的 rename(符号级语义重构)、诊断展示、自动补全**。

### P2(可砍)

17. 简易 diff/对照视图。
18. 多语言 grammar(Python/Rust 等,tree-sitter 加 grammar 即可扩展)。
19. 类型层次、书签、minimap。

---

## 7. 里程碑

> 每个里程碑结束都必须"可运行、可自测"。核心逻辑先在无 GUI 测试跑通再接 UI。
> **本版重排要点:零配置的 Source Insight 式核心体验(M1–M3)先完整落地;clangd 这个最易出问题的部分降为最后、且可选(M4),从而给项目去风险——即使 clangd 迟迟搞不定,前三个里程碑已是一个能日常用的工具。**

- **M1 — 核心骨架 + 编辑器 + 分屏 + tree-sitter 高亮/折叠/大纲 + 文本搜索 + 基础编辑**
  - 核心:`xinsight-core` 骨架、`IUiDispatcher`、`reproc`;集成 tree-sitter(c/cpp),做高亮、折叠、当前文件大纲;`SearchEngine` 的 ripgrep 文本搜索;`ProjectModel` 文件纳入规则(5.6);无 GUI 单测。
  - GUI:项目树、Tab、tree-sitter 上色的 QScintilla 视图、嵌套分屏、搜索结果面板;**基础编辑闭环——编辑、`Cmd+S` 保存、dirty 标记、关闭提示、撤销/重做、新建文件/文件夹、另存为、外部改动重载提示**(此阶段编辑尚未联动索引,索引同步在 M2)。
  - **上色即从"可加载主题结构"取色**:M1 就把 token 类别→颜色做成数据驱动结构(哪怕只填一套默认色),避免把颜色写死;这样 P1 的多主题/护眼/自定义只是"加配色文件"而非"重构上色逻辑"(同 5.3.1 的接口先行思路)。
  - **多编码读写**(2.3):M1 的编辑闭环即包含 UTF-8/GBK 检测与按原编码保存,不要留到后面(否则等于早期版本对作者自己的遗留文件不可用)。
  - 出口:打开项目即有高亮/大纲、任意分屏、`Cmd+Shift+F` 流式搜索并跳转;**能改文件并正确保存/新建**。全程零配置。
- **M2 — tree-sitter 工作区符号索引 → 零配置导航 + 编辑联动索引(Source Insight 核心,无 clangd)**
  - 核心:定义 `ISymbolIndex` 接口 + 交付 `InMemorySymbolIndex`(**v1 唯一后端**,见 5.3.1);后台建索引 + 增量更新 + 索引期间可用;接口层单测;基于索引的跳转定义、查引用(模糊)、工作区符号搜索(`Cmd+T`);`NavigationEngine` 返回栈;**编辑/保存 → tree-sitter 增量 edit → 索引切片更新的闭环(8.7),含偏移正确性测试**。
  - GUI:F12 / Shift+F12 / Cmd+T 接面板与导航;索引进度提示。
  - 出口:**在一个没有任何配置、甚至编译不过的项目上**,能按名字跳转、查引用、搜符号;**改一个符号并保存后,大纲/跳转/搜索 ≤1 秒反映新状态**;接口层单测通过且不依赖具体后端。
- **M3 — 上下文窗格(核心卖点,tree-sitter 驱动)**
  - 核心:`ContextEngine` 光标→防抖→查索引→发出源码区间;多候选排序;返回栈联动。
  - GUI:上下文窗格渲染 + drill-down + 候选切换。
  - 出口:光标扫过函数名,1 秒内下方出现其实现,**零配置**。至此已是可日常用的工具。
- **M4 — clangd 精确叠加层 + router + 打磨**
  - 核心:`LspClient` 帧解析 + 握手 + `didOpen`;`ClangdProvider`;`CodeIntelligence` 按 5.2 路由;精确定义/引用/符号/call hierarchy;compile db 检测。
  - GUI:clangd 配置诊断面板、精确/快速模式标识;会话恢复、主题、面包屑等 P1 打磨。
  - 出口:配好 compile db 后跳转/引用升级为精确;未配置则静默用 M2 的能力,无任何退化。

---

## 8. 关键技术难点与"预埋的坑"

### 8.1 tree-sitter 集成与 query

- 需要 `tree-sitter` 运行时 + `tree-sitter-c` + `tree-sitter-cpp` grammar(C++ grammar 较重但成熟),**编译进核心并打包**。
- 真正的工作量在**写/改 query(.scm)**:定义抽取、高亮、折叠各一套。**许可决策(直接采用,不再悬置)**:tree-sitter-c / tree-sitter-cpp grammar 为 MIT;若复用 nvim-treesitter 的 query,其为 Apache-2.0——**在 `THIRD_PARTY_LICENSES` 中保留 Apache-2.0 全文与署名即可合规复用**。按本项目需要在其基础上增改 query。
- **query 编写策略(v1 采用,已定)**:**定义抽取(tags)与折叠(folds)自己精简写**——本项目只需高亮/折叠/定义(函数、方法、类/结构体/枚举/联合、typedef、宏、全局变量),对着 `tree-sitter-c`/`tree-sitter-cpp` 的 `grammar.js` 里对应节点(`function_definition`/`declaration`/`struct_specifier`/`preproc_def` 等)写几十条 capture 即可,零许可负担,且"什么算一个定义"完全自控(直接决定符号索引质量,是地基,不宜依赖看不透的外部规则)。**高亮(highlights)以 nvim-treesitter 的为起点裁剪**(C++ 高亮 case 多,从零写易漏),按 Apache-2.0 保留署名。
- tree-sitter **只懂语法不懂语义**:跳转是名字匹配,天然多候选。必须实现**候选排序**(位置就近:同文件 > 同目录 > 全项目;"定义优先于声明"为尽力而为的次级排序,不作硬保证——精确区分交给 clangd)并如实呈现候选,别假装唯一。这是与 Source Insight 一致的取舍,要在 UI 上让用户理解"快速但偶尔模糊"。
- 宏:tree-sitter 看到的是宏 token 而非展开,宏定义可找到,但藏在宏体内的代码可能欠解析——对"读代码"可接受。

### 8.2 索引规模与增量

- 符号索引轻量,**v1 一律用内存后端 `InMemorySymbolIndex`**(见 5.3.1);SQLite 后端为非 v1 目标,勿在 v1 实现。存储须置于 `ISymbolIndex` 接口之后,以便将来加实现而不返工。
- 文件改动只重解析该文件、只更新其切片,避免全量重建。
- 内存后端代价:启动需重扫建索引 → M2 必须配好"后台建索引 + 进度提示 + 索引期间先给部分结果"。

### 8.3 双引擎路由的一致性

- 按 5.2 写**确定性**策略,避免 tree-sitter/clangd 结果来回跳导致闪烁;上下文窗格默认只用 tree-sitter 即时出。
- 每个结果带 `precise` 标记,UI 明确显示"精确/快速",不让用户误以为模糊结果是精确的。

### 8.4 clangd(可选层)—— 仅 M4 涉及

- **需要 `compile_commands.json`**:CMake 加 `-DCMAKE_EXPORT_COMPILE_COMMANDS=ON`;Makefile 用 `bear`/`compiledb`;**裸机 ARM** 需 `.clangd` 剥交叉编译 flag + clangd `--query-driver=/path/to/arm-none-eabi-gcc`。
- 配置诊断面板要把这些讲清楚,并强调:**没有它 XInsight 照样能用(快速模式),配了是升级到精确。**
- LSP 传输层:`Content-Length` 帧解析要处理粘包/半包;响应乱序按 `id` 匹配;处理 server→client 的 `workDoneProgress`/`publishDiagnostics`;握手 `initialize → initialized → didOpen`。**帧解析器必须单测。**

### 8.5 ripgrep `--json` 解析

- 逐行 JSON,解析 `match` 取 path/line/submatches;默认遵循 `.gitignore` 但**可关**;结果流式、可取消。

### 8.6 线程与解耦边界

- 核心阻塞 I/O 与后台索引在核心自有线程;结果经 `IUiDispatcher::post` 回 UI 线程,GUI 只在 UI 线程碰数据。
- **绝不**在 UI 线程同步等结果。
- 任何让核心 `#include <Q...>` 的写法都是架构破窗,当场拒绝。

### 8.7 文档同步与编辑一致性(v1 可编辑,故为必做项)

- **编辑 → 解析同步**:每次编辑把变更喂给 tree-sitter 的增量 `edit` 接口(传对的字节/行列偏移),再增量重解析该文件、更新其索引切片。**偏移算错会导致解析树错位**,是这里的头号坑,需针对插入/删除/多行粘贴写测试。
- **两级频率(防卡顿)**:**高亮**的增量重解析可每次按键触发(tree-sitter 足够快);但**符号索引更新要防抖或落在保存时**(如停止输入 300–500ms 后、或 `Cmd+S` 时才重建该文件符号切片),避免大文件每次按键都重建索引导致卡顿。大纲/跳转允许略微滞后于键入,保存后必须一致。
- **编辑 → clangd 同步**:clangd 在用时,编辑要发 `didChange`(带**递增 version**);保存时按需 `didSave`。未配 clangd 时此步跳过,不影响编辑。
- **保存与 dirty 状态**:每个打开的文档维护 dirty 标记;保存落盘后清除;关闭/退出时对 dirty 文档提示。多个分屏格子打开同一文件时,编辑与 dirty 状态需一致(共享同一文档缓冲,不要各存一份)。
- **撤销/重做**:每文件独立撤销栈(QScintilla 提供),保存不清空撤销历史。
- **外部改动**:检测磁盘文件被外部修改(mtime/hash),提示重新加载;本地有未保存修改时给冲突选项(见 2.3),不静默覆盖。
- **索引一致性**:保存或编辑后,该文件的符号/引用索引必须与新内容一致,保证跳转/大纲/上下文窗格不指向过期位置。

---

## 9. 非目标(明确排除,防范围蔓延)

- 不做编译 / 构建 / 运行。
- 不做调试器 / 断点 / JTAG。
- **编辑范围限定**(见 2.3):v1 编辑 = 文本编辑 + 保存 + 新建 + 撤销;**不做**自动补全、代码格式化、多光标/列编辑、宏录制、snippets。
- 不做代码生成;符号级 rename 等语义重构仅在可选 clangd 层提供(P1),不自研。
- 不做 Git 集成(仅"外部改动检测 + 提示重载",不做版本控制)。
- 不做插件系统 / 扩展市场。
- 不做远程 / SSH / 容器内浏览。
- v1 grammar 只做 C/C++;其它语言留 P2(加 grammar 扩展)。
- **打包/公证(决定注记)**:macOS Gatekeeper 签名与公证不是 v1 功能,但迟早要面对;v1 先保证本地可构建运行,分发签名留到 v1 收尾或 v2,记此一笔以免遗忘。
- 不追求超大仓库(>1000 万行)极致性能;先保证几十万行顺滑。
- 不做多 GUI 前端(架构支持,v1 只交付 Qt)。
- **不实现 SQLite 索引后端**:v1 仅交付内存后端 `InMemorySymbolIndex`,SQLite 后端为将来预留(见 5.3.1)。注意区分——存储的**接口抽象 v1 必做**,第二个**实现 v1 不做**。

---

## 10. 给 Claude Code 的实现约定(CLAUDE.md 基础)

- **语言/构建**:C++20;CMake;顶层拆 `xinsight-core`(纯逻辑,无 Qt)与 `xinsight-qt`(GUI)两个 target;`CMAKE_EXPORT_COMPILE_COMMANDS=ON`。
- **依赖**:核心 = `tree-sitter`(+c/+cpp) + `nlohmann/json` + `reproc`;GUI = Qt 6 + QScintilla;外挂 `ripgrep`、可选 `clangd`。
- **解耦铁律**:核心禁 `#include <Q...>`;CI 单独编核心+测试且不链 Qt;GUI 禁拼 LSP JSON、禁直起 rg/clangd、禁直调 tree-sitter/clangd。
- **单一出口**:代码智能只经 `CodeIntelligence`;搜索只经 `SearchEngine`;跳转只经 `NavigationEngine`;LSP 只经 `LspClient`。
- **零配置优先**:任何功能默认必须在无 clangd、无 compile db、代码编译不过的前提下可用;clangd 只做叠加增强,不得成为前置条件。
- **索引后端范围(勿擅自扩张)**:索引置于 `ISymbolIndex` 接口之后;**v1 只实现内存后端 `InMemorySymbolIndex`**,**不要实现 SQLite 后端**(见 5.3.1)。接口层单测须与后端无关。
- **编辑范围(勿擅自扩张)**:v1 编辑 = 文本编辑 + 保存 + 新建/另存为 + 撤销;**不要**实现自动补全、格式化、多光标、rename、snippets(见 2.3)。编辑必须联动 tree-sitter 增量解析与索引更新(8.7),偏移计算要有测试。
- **按里程碑推进**:一次一个 M;核心逻辑先在无 GUI 单测跑通再接 UI;每个 M 给"手动验证清单"。
- **测试重点**:tree-sitter query 抽取的符号正确性、`LspClient` 帧解析器、ripgrep JSON 解析、`NavigationEngine` 栈、`CodeIntelligence` 路由回落逻辑、编辑增量偏移(8.7)、多编码读写往返(GBK/UTF-8)。**建一个"刁钻 C/C++ 样本库"作固定夹具**(宏、模板、嵌套命名空间、K&R 风格、函数指针、跨文件同名符号)配期望符号输出——这是符号抽取长尾 bug 的最高杠杆测试资产。
- **模型分工**:架构决策 / 帧解析与并发 / 路由与索引设计 → Opus;绝大多数控件与功能实现 → Sonnet;批量改名与样板 → Haiku。
- **提交规范**:Conventional Commits + 语义化版本。

---

> **量化基准(供第 2、3、9 条判定用)**:以一个真实开源 C/C++ 库作为固定测试靶,规模约 **30–50 万行**(例如 FFmpeg 主源码树,或 Linux kernel 的某个子系统目录如 `drivers/net/`)。下列时延指标在作者的 MacBook M4 Pro 上测量。"中型项目"一律指此量级。

1. `xinsight-core` 在**不链 Qt** 下编译通过,其无界面测试对 tree-sitter 零配置跳转、对真实 clangd 精确跳转、对 ripgrep 搜索均通过。
2. **零配置铁律**:打开基准库(**无 `compile_commands.json`、无 clangd、且不要求能编译**)——高亮、大纲、按名字跳转、查引用、工作区符号搜索、上下文窗格**全部可用**;冷启动全量索引 **≤ 30 秒**(索引期间界面不卡、已索引部分即可用)。
3. **响应指标**:光标扫过函数名,上下文窗格 **≤ 1 秒**出现其实现(tree-sitter 驱动,不等配置);`Cmd+T` 符号搜索输入到出结果 **≤ 200ms**;跳转定义 **≤ 200ms**。
4. 能分成"左右两列,右列再上下两格",三文件同屏对照。
5. `Cmd+Shift+F` 全项目文本搜索流式出结果、可取消;`Cmd+T` 按名字找到全项目符号;结果双击跳转、返回栈一致。
6. **精确叠加**:对一个配好 compile db(含 STM32 裸机 `.clangd` + `--query-driver`)的项目,启用 clangd 后跳转/引用升级为精确,UI 显示"精确"模式;移除配置后自动回落"快速"模式且无退化。
7. 关闭重开,文件与分屏布局自动恢复。
8. **索引后端范围**:索引经 `ISymbolIndex` 接口访问,v1 交付且仅交付 `InMemorySymbolIndex`;接口层单测与后端无关(不 `#include` 任何具体后端头文件)、全部通过;代码库中不存在 SQLite 后端实现。
9. **编辑闭环**:在基准库中打开一个文件,改一个函数名/加一个函数并 `Cmd+S` 保存——磁盘正确落盘;该文件的大纲、按名字跳转、上下文窗格 **≤ 1 秒**内反映改动后的符号(索引已同步);`Cmd+Z` 能撤销;dirty 标记、关闭提示、外部改动重载提示均按 2.3 生效;新建文件/文件夹与另存为可用。
10. **多编码往返**:打开一个 **GBK 编码带中文注释**的 `.c` 文件——正确显示不乱码,编辑后 `Cmd+S` **仍以 GBK 写回**且中文注释完好;UTF-8 文件同样往返无损。

做到以上十条,即视为一个"能替代 Source Insight 日常看代码、且在其零配置软肋上不输、支持轻量编辑、在跨平台上胜出"的可用 v1。
